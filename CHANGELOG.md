# Changelog

## 0.1.0

- 支持 `/gps list` 列出项目
- 支持 `/gps -m "消息"` 对默认项目执行 `git add .` → `git commit -m` → `git push`
- 支持 `/gps 项目名 -m "消息"` 对指定项目执行同样流程
- 保留 `/gpl` 的 `git pull` 能力
- 添加默认项目标记、帮助文案和发布所需打包配置
